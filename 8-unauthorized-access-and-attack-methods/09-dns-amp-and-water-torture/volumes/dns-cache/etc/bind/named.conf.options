options {
  directory "/var/cache/bind";
  //directory "/etc/bind";
  listen-on port 53 { 127.0.0.1; 192.168.1.12; };
  allow-query { any; }; // どこからでもクエリを受け付ける

  recursion yes; // キャッシュとして持っていないデータはDNSコンテンツサーバに問い合わせる

  dnssec-validation no;
}